import React from "react";
import ReactDOM from "react-dom/client";
import MemoryMatchingGame from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MemoryMatchingGame />
  </React.StrictMode>
);
